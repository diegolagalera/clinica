# =============================================
#  CUSPIA-ERP — Nginx Reverse Proxy
#  Handles: SSL termination, subdomain routing,
#           WebSocket proxying, static file serving
# =============================================

events {
    worker_connections 1024;
}

http {
    # ---------- General ----------
    sendfile on;
    keepalive_timeout 65;
    client_max_body_size 50M;

    # Logging
    log_format main '$remote_addr - $remote_user [$time_local] '
                    '"$request" $status $body_bytes_sent '
                    '"$http_referer" "$http_user_agent"';
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    # ---------- Docker DNS resolver ----------
    # Enables dynamic re-resolution of container hostnames.
    # Without this, nginx caches IPs at startup and breaks
    # when containers are recreated (e.g. after deploy).
    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;

    # ---------- Rate limiting ----------
    limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;

    # ---------- HTTP → HTTPS redirect ----------
    server {
        listen 80;
        server_name _;

        # Let's Encrypt challenge
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Redirect all other HTTP to HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }

    # ---------- API Backend (api.cuspia.com) ----------
    server {
        listen 443 ssl;
        server_name api.cuspia.com;

        ssl_certificate /etc/letsencrypt/live/cuspia.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/cuspia.com/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # API requests — use variable to force DNS re-resolution
        location / {
            limit_req zone=api burst=50 nodelay;

            set $backend_upstream http://backend:3000;
            proxy_pass $backend_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WebSocket (Socket.io)
        location /socket.io/ {
            set $backend_upstream http://backend:3000;
            proxy_pass $backend_upstream;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 86400;
        }
    }

    # ---------- Frontend (app.cuspia.com + *.cuspia.com subdomains) ----------
    server {
        listen 443 ssl;
        server_name app.cuspia.com *.cuspia.com;

        ssl_certificate /etc/letsencrypt/live/cuspia.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/cuspia.com/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        location / {
            set $frontend_upstream http://frontend:80;
            proxy_pass $frontend_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
